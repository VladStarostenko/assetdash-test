{
  "name": "assetdash-monorepo",
  "private": true,
  "engines": {
    "node": ">=10.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "wsrun -c lint",
    "build": "wsrun -te -c build",
    "start": "wsrun -c start",
    "test": "wsrun -c test"
  },
  "devDependencies": {
    "wsrun": "^5.2.0"
  }
}
